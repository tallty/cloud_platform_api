<%= form_for(appointment) do |f| %>
  <% if appointment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(appointment.errors.count, "error") %> prohibited this appointment from being saved:</h2>

      <ul>
      <% appointment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %>
    <%= f.text_field :user_id %>
  </div>

  <div class="field">
    <%= f.label :interface_document_id %>
    <%= f.text_field :interface_document_id %>
  </div>

  <div class="field">
    <%= f.label :aasm_state %>
    <%= f.text_field :aasm_state %>
  </div>

  <div class="field">
    <%= f.label :start_time %>
    <%= f.date_select :start_time %>
  </div>

  <div class="field">
    <%= f.label :end_time %>
    <%= f.date_select :end_time %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
